FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY games_demo/ ./games_demo/

EXPOSE 5000

CMD ["uvicorn", "games_demo.app:app", "--host", "0.0.0.0", "--port", "5000"]
