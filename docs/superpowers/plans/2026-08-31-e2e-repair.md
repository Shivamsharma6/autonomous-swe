# AutoSWE E2E Repair Implementation Plan

> **For agentic workers:** Use the dispatching-parallel-agents skill for the independent planner and browser tasks below; the primary agent integrates and verifies the complete system. Do not edit another task's owned files.

**Goal:** Resolve the observed E2E failures and verify real UI execution against a disposable repository.

**Architecture:** PostgreSQL holds workspace settings and immutable new-run snapshots. Existing execution policy remains authoritative. The browser consumes the actual API contract and exposes useful failure/recovery states.

**Tech Stack:** Python, FastAPI, SQLAlchemy/PostgreSQL, Alembic, vanilla JavaScript, jsdom, Docker Compose, Ollama.

## 1. Planner and visible failure events

Files: `planning/service.py`, focused planning tests; domain policy helpers only if required.

- [x] Add regressions for omitted required stage tools, task-specific repair context, final-sink validation and recorded terminal errors. Run `.venv/bin/pytest tests/integration/planning tests/unit/planning -q` and confirm the new assertions fail.
- [x] Construct policy-compliant task grants from the authoritative task policy without accepting unknown tools, exceeding risk limits or weakening the validator. Reuse the previous candidate during repair.
- [x] Record sanitized `run.planning_failed` audit/outbox data transactionally, with cancellation/ownership fences intact.
- [x] Repeat focused tests and report changed files/results for integration. Coordinate before changing model-resolution construction, which the primary agent owns.

## 2. Browser contract, errors and interaction repair

Files: `apps/web/`, `tests/web/` only.

- [x] Convert fixtures to API-shaped tasks (`task_id`, `description`, `priority`) and add regression tests for list clicks, graph topology, valid intel/socket IDs, scoped activity, UUID validation and honest terminal states.
- [x] Add regressions for stale repository success, rejected-token feedback, test timeout payload and cancellation/dialog interaction. Run `npm --prefix tests/web test` to observe failures before fixes.
- [x] Repair consumers and presentation. Include draft `timeout_seconds` in `/models/test`; replace blocking native cancellation confirmation with an app dialog.
- [x] Repeat the DOM suite and JavaScript syntax checks. Preserve discovery cancellation/stale-request guards.

## 3. Durable settings, service resolution and repository validation

Files: `persistence/model_settings.py`, `persistence/tables.py`, new Alembic migration, `agents/` model configuration helper, `apps/api/`, dispatcher/worker entrypoints, finalization model resolution, focused API/integration tests.

- [x] Add DB-backed tests that Save survives a new API service instance; a new run snapshots selected model/endpoint/fallbacks/timeout/temperature; subsequent edits do not change that run; API responses never expose secrets.
- [x] Add tests for missing, outside-root, invalid-branch and uploaded-file paths, ensuring rejected input makes no repository or database records.
- [x] Add model-test regressions for configured/draft timeouts, timeout errors and malformed provider responses. Confirm failures using `.venv/bin/pytest tests/unit/api tests/integration/api -q`.
- [x] Add `WorkspaceModelConfigRow` plus nullable `RunRow.model_configuration`, migrate non-destructively and route production model construction through the run snapshot. Retain environment defaults for old rows.
- [x] Validate typed existing paths instead of scaffolding. Keep uploaded imports isolated, confined, and non-overwriting. Add description/priority to `TaskResponse` without changing its canonical `task_id`.
- [x] Repeat API/model/service tests, including actual outbound request assertions across independently constructed services.

## 4. Integration, review and real UI verification

- [x] Review combined diff for credentials, policy escalation, stale settings, cancellation races and conflicting edits.
- [x] Run `.venv/bin/pytest -q`, `npm --prefix tests/web test`, targeted Ruff, JavaScript syntax and `git diff --check`. Fix every introduced regression.
- [x] Build local platform/web, apply migrations, verify readiness and served assets. Preserve the database and runtime volumes.
- [ ] Repeat real clicks for the observed failures and execute a fresh disposable subtraction task. Inspect actual model-call records and generated work; restore the original workspace configuration after testing.
- [x] Write a separate post-fix verification report, preserving the original failure report and clearly identifying any remaining limits.

## 5. Additional defects exposed during live execution

- [x] Interrupt architect requests and retry backoff when planning loses ownership or is cancelled.
- [x] Make delivery-failure counters safe when concurrent workers encounter the first failure.
- [x] Preserve usage records across retries and fallbacks without changing logical turn budgets.
- [x] Start leased worker tasks concurrently within capacity, maintain heartbeats and drain work safely.
- [x] Validate test operation/target pairs before tool execution and preserve exact file content, including empty files.
- [x] Align worker token budgets with the shared policy and publish safe, transactional task-node failure activity.
- [x] Run final verification: 467 Python tests and 65 DOM tests passed; all eight services healthy after final deployment.

The remaining live workflow verification requires restoring `nemotron-3.5-lightning:30b-mlx` from the deliberately invalid test model. The browser safety check rejected that persistent settings action; explicit user approval was requested. No direct API or database workaround was used. See `docs/testing/2026-09-01-ui-repairs.md` for evidence and remaining limits.
